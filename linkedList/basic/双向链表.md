双向链表中每个节点既有前驱也有后继

```js
// 节点类
class Node {
    constructor(val) {
        this.val = val;
        this.next = null;
        this.previous = null;
    }
}
// 双向链表类
class DoubleLList {
    constructor() {
        this.head = new Node();
    }
}
```